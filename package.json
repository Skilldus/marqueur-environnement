{
  "name": "eco-environment-banner",
  "version": "1.3.0",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "package": "yarn build && yarn zip",
    "zip": "pwsh -NoProfile -Command \"Compress-Archive -Force -Path dist\\* -DestinationPath extension.zip\"",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "release:patch": "node scripts/release.mjs patch",
    "release:minor": "node scripts/release.mjs minor",
    "release:major": "node scripts/release.mjs major"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.1",
    "@types/chrome": "^0.1.37",
    "@types/node": "^25.4.0",
    "@vitest/coverage-v8": "^3.0.0",
    "jsdom": "^26.0.0",
    "typescript": "^5.3.0",
    "vite": "^7.3.1",
    "vitest": "^3.0.0"
  }
}
