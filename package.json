{
  "name": "eco-environment-banner",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "package": "yarn build && yarn zip",
    "zip": "cd dist && powershell Compress-Archive -Path * -DestinationPath ../extension.zip -Force"

  },
  "devDependencies": {
    "@types/chrome": "^0.1.37",
    "@types/node": "^25.4.0",
    "typescript": "^5.3.0",
    "vite": "^7.3.1"
  }
}
